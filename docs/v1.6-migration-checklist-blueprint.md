# 🎯 Blueprint Mode Extraction Checklist

## Pre-Migration Verification
- [ ] Capture screenshot of current Blueprint mode UI
- [ ] Document all 15+ Blueprint fields
- [ ] Map platform-specific conditional logic
- [ ] List all media generator toggles
- [ ] Record aspect ratio/codec combinations
- [ ] Note all platform presets
- [ ] Document section organization

---

## 📝 Blueprint Mode Field Preservation Map

### Section 1: Media Basics
```
Fields to Extract:
1. title* (required)
   - Label: "Prompt Title"
   - Helper: "Name for this media prompt"
   - Example: "e.g., Product Hero Shot, Explainer Video..."
   - Validation: min(5), max(200)
   - Tooltip: Present

2. description* (required)
   - Label: "Detailed Description"
   - Helper: "Describe what you want to create"
   - Example: "e.g., A cinematic drone shot of mountains at sunset..."
   - Validation: min(20), max(2000)
   - Component: Textarea (rows: 8)
   - Tooltip: Present

3. mediaStyle (required)
   - Label: "Media Style / Genre"
   - Helper: "Define creative or cinematic style"
   - Example: "e.g., cinematic, photorealistic, anime, watercolor..."
   - Validation: min(5), max(500)
   - Tooltip: Present
```

### Section 2: Generator Settings (NEW v1.5)
```
Fields to Extract:
4. mediaGenerator (optional, NEW)
   - Label: "Media Generator"
   - Helper: "Tool or platform you'll use"
   - Example: "e.g., Midjourney v6, DALL-E 3, Stable Diffusion..."
   - Validation: min(2), max(100)
   - Tooltip: Present

5. mediaAgent (optional, NEW)
   - Label: "Media Agent"
   - Helper: "Creative role or perspective"
   - Example: "e.g., Film Director, Concept Artist, VFX Supervisor..."
   - Validation: min(2), max(100)
   - Tooltip: Present
```

### Section 3: Output Specifications
```
Fields to Extract:
6. platform* (select, required)
   - Label: "Target Platform"
   - Options: midjourney, dall-e, stable-diffusion, runway, pika, eleven-labs, suno, udio, heygen, d-id
   - Helper: "Select your media generation platform"
   - Default: midjourney
   - TRIGGERS: Platform-specific fields

7. resolution (select)
   - Label: "Resolution"
   - Options: 512x512, 1024x1024, 1920x1080, 3840x2160, 4096x4096
   - Default: 1024x1024
   - Helper: "Output dimensions"

8. aspectRatio (select)
   - Label: "Aspect Ratio"
   - Options: 1:1, 16:9, 9:16, 4:3, 3:4, 21:9, custom
   - Default: 16:9
   - Helper: "Dimensions for your media output"
   - Test ID: select-media-aspect-ratio (deduplicated)

9. fileType (select)
   - Label: "File Type"
   - Options: jpg, png, webp, mp4, mov, wav, mp3
   - Default: jpg
   - Helper: "Format for the final media file"
   - Test ID: select-media-file-type (deduplicated)

10. exportUseCase (select)
    - Label: "Export Use Case"
    - Options: social-media, print, web, presentation, video-production
    - Default: social-media
    - Helper: "Where will this media be used?"

11. packagingPreference (select)
    - Label: "Packaging Preference"
    - Options: single-file, batch, layered, animated-sequence
    - Default: single-file
    - Helper: "How should files be delivered?"
```

### Section 4: Advanced Settings
```
Fields to Extract:
12. mood (optional)
    - Label: "Mood / Emotion"
    - Helper: "Describe the intended vibe"
    - Example: "e.g., Hopeful, Energetic, Mysterious, Dreamy..."
    - Validation: max(500)
    - Component: Textarea (rows: 3)

13. useCase (optional)
    - Label: "Use Case"
    - Helper: "What will this media be used for?"
    - Example: "e.g., YouTube intro, Social media ad, Product showcase..."
    - Validation: max(500)
    - Component: Textarea (rows: 3)
```

### Section 5: Platform-Specific Fields (Dynamic)
```
Conditional Fields (based on platform selection):

IF platform = 'midjourney':
  - version: select [v5, v5.1, v5.2, v6]
  - stylize: slider [0-1000]
  - chaos: slider [0-100]

IF platform = 'dall-e':
  - quality: select [standard, hd]
  - style: select [vivid, natural]

IF platform = 'stable-diffusion':
  - model: select [sd-1.5, sd-2.1, sdxl]
  - steps: slider [1-150]
  - cfgScale: slider [1-30]

IF platform = 'runway':
  - duration: slider [1-16] seconds
  - motion: slider [1-10]

IF platform = 'pika':
  - fps: select [8, 12, 24]
  - duration: slider [3-6] seconds

IF platform = 'eleven-labs':
  - voice: select [list of voices]
  - emotion: select [neutral, happy, sad, angry]

IF platform = 'suno' OR 'udio':
  - genre: text input
  - tempo: select [slow, medium, fast]
  - duration: slider [8-180] seconds
```

---

## ✅ Blueprint Mode Acceptance Criteria

### Visual Preservation
- [ ] Card component with p-6 padding maintained
- [ ] Section header with purple dot indicator (Blueprint-specific)
- [ ] Grid layout for output specs (md:grid-cols-2)
- [ ] Platform-specific fields appear/hide correctly
- [ ] Helper text in muted style
- [ ] Example text in italic

### Interaction Preservation
- [ ] Platform selector triggers conditional fields
- [ ] Resolution/aspect ratio sync logic
- [ ] File type changes based on media type
- [ ] Sliders for platform parameters work
- [ ] Form validation on submit
- [ ] Tooltip hover behavior

### Dynamic Rules Preservation
```
Blueprint-Specific Logic:
1. Platform selection → Show/hide platform fields (CRITICAL)
2. Video platforms → Enable video file types only
3. Audio platforms → Enable audio file types only
4. Aspect ratio 9:16 → Suggest mobile use case
5. Resolution > 2K → Warning about file size
6. Midjourney selected → Default chaos to 0
7. Test ID deduplication → media-file-type, media-aspect-ratio
```

---

## 🔍 Blueprint Mode Design Drift Checkpoints

### Critical Elements to Verify

1. **Field Order Integrity**
   ```
   Expected sequence:
   1. Media Basics (3 fields)
   2. Generator Settings (2 fields)
   3. Output Specs (6 fields)
   4. Advanced (2 fields)
   5. Platform-Specific (varies)
   Total: 13+ Blueprint fields
   ```

2. **Visual Consistency**
   - Section spacing: gap-6 between cards
   - Grid layout: md:grid-cols-2 for output specs
   - Purple dot for section headers
   - Platform fields in collapsible section

3. **Dynamic Toggles**
   - Platform selector state management
   - Conditional field rendering logic
   - Parameter range validations
   - Default value cascading

4. **Test ID Uniqueness**
   - select-media-file-type (not select-file-type)
   - select-media-aspect-ratio (not select-aspect-ratio)
   - All platform-specific fields unique

---

## 🔄 Blueprint Mode Migration Sequence

### Phase 1: Component Setup
1. Create BlueprintForm component wrapper
2. Import media-specific UI components
3. Setup platform configuration map

### Phase 2: Field Migration
1. Move Media Basics section (title, description, mediaStyle)
2. Move Generator Settings (mediaGenerator, mediaAgent)
3. Move Output Specs grid (6 fields)
4. Move Advanced section (mood, useCase)
5. Move Platform-specific conditional logic

### Phase 3: Logic Migration
1. Extract platform toggle logic
2. Move aspect/resolution sync
3. Preserve file type filtering
4. Migrate platform presets
5. Connect conditional field rendering

### Phase 4: Verification
1. Test all 10+ platform toggles
2. Verify conditional field display
3. Check slider/select interactions
4. Validate grid responsiveness
5. Test ID uniqueness

---

## ✅ Blueprint Subcomponent Status

**Component Name:** BlueprintForm.tsx  
**Estimated Lines:** ~600  
**Base Field Count:** 13  
**Platform Fields:** 3-7 per platform  
**Sections:** 5  
**Validation Rules:** 10+  
**Dynamic Rules:** 7+  
**Status:** STAGED FOR EXTRACTION